P_ADIDAS_ADA16 ADIDAS_KEY "SELECT adidas_key, member||ind||family||counter||subdomain||domain name FROM p_adidas_names"
P_CAN_NODE CAN_SEG_KEY "SELECT can_seg_key, 'Seg:' || can_seg_key || ',' || baud || ' Baud' AS seg FROM p_can_seg"
P_CAN_NODE NODE_KEY   "SELECT selectron_key AS key, name FROM selectron_names union SELECT ec_key AS key, name FROM ec_names union SELECT node_key AS key, name || ':' || port FROM p_can_node_to_ioc_port nip, p_ioc_port ip, ioc_names ion WHERE ion.ioc_key=ip.ioc_key AND ip.port_key=nip.port_key"
P_INSERTION IOC_KEY    "SELECT ioc_key,name FROM ioc_names"
P_INSERTION NAME_KEY   "SELECT key,name FROM id_names"
P_INSERTION_EPICS_POS CIOC_PORT_KEY "SELECT p.port_key, n.name || ',Port ' || p.port AS ioc_port FROM p_ioc_port p ,ioc_names n WHERE n.ioc_key=p.ioc_key"
P_INSERTION_EPICS_POS IDIOC_PORT_KEY "SELECT p.port_key, n.name || ',Port ' || p.port AS ioc_port FROM p_ioc_port p ,ioc_names n WHERE n.ioc_key=p.ioc_key"
P_INSERTION_EPICS_POS INSERTION_KEY "SELECT insertion_key,name FROM id_names"
P_INSERTION_VALUE INSERTION_KEY "SELECT insertion_key,value FROM p_insertion_value WHERE name='undulator'"
P_IOC      NAME_KEY   "SELECT key,name FROM ioc_names"
P_IOC      RACK_KEY   "SELECT rack_key,name FROM p_rack"
P_IOC      TERMINAL_SERVER_KEY "SELECT t.terminal_server_key,n.name FROM p_terminal_server t,names n WHERE t.name_key=n.key"
P_IOC_PORT IOC_KEY    "SELECT ioc_key,name FROM ioc_names"
P_NAME     FAMILY_KEY "SELECT family_key,part_family FROM p_family"
P_NAME     SUBDOMAIN_KEY "SELECT s.subdomain_key, s.part_subdomain || s.part_postfix || d.part_domain AS subdomain FROM p_subdomain s, p_domain d WHERE s.domain_key=d.domain_key"
P_SIMPLE   IOC_KEY    "SELECT ioc_key,name FROM ioc_names"
P_SIMPLE   NAME_KEY   "SELECT names.key,names.name FROM names,p_simple WHERE names.key=p_simple.name_key"
P_SIMPLE   SIMPLE_TYPE_KEY "SELECT simple_type_key,type_name FROM p_simple_type"
P_SIMPLE_EPICS SIMPLE_KEY "SELECT simple_key,signal FROM p_simple"
P_SIMPLE_EPICS SIMPLE_AUTH_EPICS_KEY "SELECT a.simple_epics_auth_key, g.name || ',' || a.access_type FROM p_simple_epics_auth a, base.tbl_person_group g WHERE a.person_group_key=g.person_group_key"
P_SIMPLE_EPICS SIMPLE_EPICS_TYPE_KEY "SELECT simple_epics_type_key,description FROM p_simple_epics_type"
P_TERMINAL_SERVER NAME_KEY   "SELECT key,name FROM names WHERE member='TS'"
P_TRD      TRD_KEY    "SELECT trd_key, member||ind||family||counter||subdomain||domain name FROM p_trd_names"
P_TRD      BASE_TRIGGER "SELECT trd_key, member||ind||family||counter||subdomain||domain name FROM p_trd_names"
P_TRD_CABLE TRD_KEY    "SELECT trd_key, member||ind||family||counter||subdomain||domain name FROM p_trd_names"
P_TRD_GROUP IOC_KEY    "SELECT ioc_key,name FROM ioc_names"
P_TRD_GROUP TRD_GROUP_KEY "SELECT trd_group_key,name FROM p_trd_group"
P_TRD_GROUP_TO_GROUP GROUP_KEY  "SELECT trd_group_key,name FROM p_trd_group"
P_TRD_GROUP_TO_GROUP TRD_GROUP_KEY "SELECT trd_group_key,name FROM p_trd_group"
P_TRD_NAMES TRD_KEY    "SELECT trd_key, member||ind||family||counter||subdomain||domain  name FROM p_trd_names"
P_TRD_TO_GROUP TRD_GROUP_KEY "SELECT trd_group_key,name FROM p_trd_group"
P_TRD_TO_GROUP TRD_KEY    "SELECT trd_key, member||ind||family||counter||subdomain||domain  name FROM p_trd_names"
TBL_ADIDAS_ADA16 ADIDAS_KEY "SELECT adidas_key, member||ind||family||counter||subdomain||domain name FROM tbl_adidas_names"
TBL_CAN_NODE CAN_SEG_KEY "SELECT can_seg_key, 'Seg:' || can_seg_key || ',' || baud || ' Baud' AS seg FROM tbl_can_seg"
TBL_CAN_NODE NODE_KEY   "SELECT selectron_key AS key, name FROM selectron_names union SELECT ec_key AS key, name FROM ec_names union SELECT node_key AS key, name || ':' || port FROM tbl_can_node_to_ioc_port nip, tbl_ioc_port ip, v_ioc_names ion WHERE ion.ioc_key=ip.ioc_key AND ip.port_key=nip.port_key"
TBL_DOMAIN FACILITY_KEY "SELECT facility_key,name FROM tbl_facility"
TBL_INSERTION IOC_KEY    "SELECT ioc_key,name FROM v_ioc_names"
TBL_INSERTION NAME_KEY   "SELECT key,name FROM v_insertion_names"
TBL_INSERTION_EPICS_POS CIOC_PORT_KEY "SELECT p.port_key, n.name || ',Port ' || p.port AS ioc_port FROM tbl_ioc_port p ,v_ioc_names n WHERE n.ioc_key=p.ioc_key"
TBL_INSERTION_EPICS_POS IDIOC_PORT_KEY "SELECT p.port_key, n.name || ',Port ' || p.port AS ioc_port FROM tbl_ioc_port p ,v_ioc_names n WHERE n.ioc_key=p.ioc_key"
TBL_INSERTION_EPICS_POS INSERTION_KEY "SELECT insertion_key,name FROM v_insertion_names"
TBL_INSERTION_VALUE INSERTION_KEY "SELECT insertion_key,value FROM tbl_insertion_value WHERE name='undulator'"
TBL_IOC      NAME_KEY   "SELECT key,name FROM v_ioc_names"
TBL_IOC      RACK_KEY   "SELECT rack_key,name FROM tbl_rack"
TBL_IOC      TERMINAL_SERVER_KEY "SELECT t.terminal_server_key,n.name FROM tbl_terminal_server t,names n WHERE t.name_key=n.key"
TBL_IOC_PORT IOC_KEY    "SELECT ioc_key,name FROM v_ioc_names"
TBL_NAME     FAMILY_KEY "SELECT family_key,part_family FROM tbl_family"
TBL_NAME   SUBDOMAIN_KEY "SELECT s.subdomain_key, s.part_subdomain || s.part_postfix || d.part_domain || f.part_facility AS subdomain FROM tbl_subdomain s, tbl_domain d, tbl_facility f WHERE s.domain_key=d.domain_key AND d.facility_key = f.facility_key"
TBL_SIMPLE   IOC_KEY    "SELECT ioc_key,name FROM v_ioc_names"
TBL_SIMPLE   NAME_KEY   "SELECT names.key,names.name FROM names,tbl_simple WHERE names.key=tbl_simple.name_key"
TBL_SIMPLE   SIMPLE_TYPE_KEY "SELECT simple_type_key,type_name FROM tbl_simple_type"
TBL_SIMPLE_EPICS SIMPLE_KEY "SELECT simple_key,signal FROM tbl_simple"
TBL_SIMPLE_EPICS SIMPLE_AUTH_EPICS_KEY "SELECT a.simple_epics_auth_key, g.name || ',' || a.access_type FROM tbl_simple_epics_auth a, base.tbl_person_group g WHERE a.person_group_key=g.person_group_key"
TBL_SIMPLE_EPICS SIMPLE_EPICS_TYPE_KEY "SELECT simple_epics_type_key,description FROM tbl_simple_epics_type"
TBL_SUBDOMAIN DOMAIN_KEY "SELECT d.domain_key, d.part_domain || f.part_facility AS domain FROM tbl_domain d, tbl_facility f WHERE d.facility_key = f.facility_key"
TBL_TERMINAL_SERVER NAME_KEY   "SELECT key,name FROM names WHERE member='TS'"

